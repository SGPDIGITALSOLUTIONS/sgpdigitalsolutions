import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/portfolio/i-care-services');

export default function PageLayout({ children }: { children: React.ReactNode }) {
  return children;
}
